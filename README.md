# Cyclopoid 

```
# Launch MySQL in a docker container
$ docker-compose -f docker-compose.yml up -d
```

```
# Build project
$ ./gradlew build

# Launch application
$ ./gradlew bootRun 

# Launch unit tests
$ ./gradlew test
```
